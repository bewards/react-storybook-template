import { defineMessages } from 'react-intl'

const scope = 'react-best-practices.components.StatusToggle'

export default defineMessages({
  filter: {
    id: `${scope}.filter`,
    defaultMessage: 'Filter',
  },
})
