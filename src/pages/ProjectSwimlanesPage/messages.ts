import { defineMessages } from 'react-intl'

const scope = 'react-best-practices.pages.ProjectSwimlanes'

export default defineMessages({
  addNewProject: {
    id: `${scope}.addNewProject`,
    defaultMessage: 'Add New Project',
  },
})
